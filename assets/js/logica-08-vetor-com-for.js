function exemplo01() {
    //Solicitar o nome de 3 pessoas e apresentar

    let nomes = [];
    // i = i + 1;
    // i += 1;
    // i++;

    for (let i = 0; i < 3; 1 += 1) {
        nomes.push(prompt("Digite o nome"));
    }

    for (let i = 0; i < 3; i += 1) {
        alert(nomes[1]);
    }
}